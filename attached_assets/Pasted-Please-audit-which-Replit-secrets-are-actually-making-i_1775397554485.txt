Please audit which Replit secrets are actually making it into the Surge iOS build and which are not.

Context:
- Some secrets exist in Replit, but TestFlight showed RevenueCat key was missing at runtime
- We need a systematic audit of build-time vs runtime secret availability
- Do NOT change business logic yet unless needed for diagnosis

Please do the following:

1. Inventory all relevant Surge secrets currently referenced in the codebase, including at least:
   - EXPO_PUBLIC_REVENUECAT_IOS_API_KEY
   - EXPO_PUBLIC_REVENUECAT_TEST_API_KEY
   - EXPO_PUBLIC_ADMOB_IOS_APP_ID
   - EXPO_PUBLIC_ADMOB_ANDROID_APP_ID
   - any other EXPO_PUBLIC_* keys used by Surge

2. Search the repo and summarize for each secret:
   - where it is read
   - whether it is used in app.config.js
   - whether it is used only at runtime in app code
   - whether it is passed through expo.extra
   - whether it is used in plugin config

3. For Surge specifically, identify which secrets are:
   - present in resolved Expo config (`APP_VARIANT=surge npx expo config --type public`)
   - missing from resolved Expo config
   - likely available only in Replit shell but not in TestFlight runtime

4. Add temporary diagnostics to the app for Surge only:
   - show whether RevenueCat key comes from expo.extra or process.env
   - show safe prefix only (first 6–8 chars)
   - show whether AdMob IDs are present
   - show bundle ID and execution environment

5. Recommend the correct transport mechanism for each secret:
   - plugin config
   - expo.extra
   - runtime env only
   - do not expose to client

6. Output a clear table:
   - Secret name
   - Used where
   - Present in shell? unknown/yes/no
   - Present in expo config? yes/no
   - Present at runtime? yes/no/likely
   - Fix needed? yes/no
   - Recommended fix

7. Then implement the minimal fix for Surge RevenueCat only:
   - inject EXPO_PUBLIC_REVENUECAT_IOS_API_KEY into expo.extra
   - read it from Constants.expoConfig?.extra at runtime
   - keep existing diagnostics

Do not change product IDs.
Do not change paywall business logic.
Focus on build/runtime secret visibility.